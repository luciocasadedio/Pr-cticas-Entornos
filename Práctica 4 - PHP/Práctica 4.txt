Práctica 4 - PHP
Ejercicio 1:  En el siguiente código identificar: 
• las variables y su tipo 
• los operadores 
• las funciones y sus parámetros 
• las estructuras de control 
• cuál es la salida por pantalla 

<?php function doble($i) {    
     return $i*2;
      } 
    $a = TRUE;    
    $b = "xyz";    
    $c = 'xyz';   
    $d = 12;      
    echo gettype($a);   
    echo gettype($b);    
    echo gettype($c); 
    echo gettype($d);  
    if (is_int($d)) {     
        $d += 4; } 
        if (is_string($a)) {     
            echo "Cadena: $a"; } 
    $d = $a ? ++$d : $d*3; 
    $f = doble($d++); 
    $g = $f += 10; 
    echo $a, $b, $c, $d, $f , $g; 
    ?>
    /// Tipos de variables:
    La variable $a de tipo booleana, la variable $b de tipo string, la variable $c de tipo string, la variable $d de tipo integer, i de tipo integer,
    la variable $f de tipo integer, y la variable $g de tipo integer.
    /// Operadores: 
    Los operadores usados son el unario ++,+=,y el de asignación =, los operadores binarios *,+, y el operador ternario ? en la línea 24 de este archivo txt.
    /// Funciones:
    las funciones usadas son function doble donde se manda por parámetro la variable $i y se duplica su valor, la función gettype que obtiene el tipo de dato de las variables
    $a,$b,$c,$d, la función is_int() que le envía por parámetero la variable $d para ver si es un entero, la función is_string que le envia por parámetro $a y determina si es un string.
    /// Estructuras de control:
    las estructuras de control usadas son dos if que preguntan si es una variable int y otro que pregunta si la variable es de tipo string.
    /// Salida:
    La salida por pantalla es: 1,xyz,xyz,18,44,54

Ejercicio 2: Indicar si los siguientes códigos son equivalentes
a) 